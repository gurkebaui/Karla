# Karla - The Self-Evolving Reasoner
# Requirements for training and inference

# Core ML
torch>=2.0.0
transformers>=4.36.0
accelerate>=0.25.0
bitsandbytes>=0.41.0  # For 4-bit quantization
safetensors>=0.4.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0
datasets>=2.14.0
tokenizers>=0.14.0

# Training utilities
wandb>=0.16.0  # Optional: for experiment tracking
tensorboard>=2.15.0
tqdm>=4.66.0

# Optimization
scipy>=1.11.0
scikit-learn>=1.3.0

# Development
pytest>=7.4.0
black>=23.0.0
isort>=5.12.0
flake8>=6.1.0

# Optional: For advanced features
# flash-attn>=2.0.0  # Flash Attention (requires specific CUDA)
# triton>=2.1.0  # For custom kernels
